# Style Guide

## CSS

```
element {
    /* 1 - Positioning (position, top, bottom, left, right, z-index, etc.) */
    /* 2 - Display & Layout (display, visibility, flex, grid, etc.) */
    /* 3 - Box Model (width, height, margin, padding, border, overflow, etc.) */
    /* 4 - Color (background, foreground) */
    /* 5 - Font (family, decoration, size, etc.) */
    /* 6 - Cursor */
    /* 7 - Animations (transform, translate, transition, animation, etc.) */
}
```
